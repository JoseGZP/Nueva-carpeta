
	/*
	if((peso < 50) && (precio > 75)){
		costo = 0;
		//cout << nombre << " " << cedula << " " << peso << " " << precio << " " << costo;
	}
	else{
	if(peso > 50){
			y = (-9.21)*peso;
			x = pow(1.01, y);
			costo = 25*(1 - x);
			//cout << nombre << " " << cedula << " " << peso << " " << precio << " " << costo;	
	}
	}
	if((peso < 50) && (precio <= 75) && (precio > 50)){
	costo = 5;
			//cout << nombre << " " << cedula << " " << peso << " " << precio << " " << costo;
	}
	else{
	if((peso < 50) && (precio <= 50) && (precio > 25)){
	costo = 10;
	}
	}
	if((peso < 50) && (precio <= 75) && (precio > 50)){
	costo = 5;
	}
	else{
	if((peso < 50) && (precio <= 25) && (precio >= 0)){
	costo = 15;
	}
	} */
	
	/*if((peso < 50) && (precio <= 50) && (precio > 25)){
			costo = 10;
			//cout << nombre << " " << cedula << " " << peso << " " << precio << " " << costo;
	} */
/*	else if((peso < 50) && (precio <= 25) && (precio >= 0)){
			costo = 15;
			//cout << nombre << " " << cedula << " " << peso << " " << precio << " " << costo;
	}
	
	if(peso > 50){
			y = (-9.21)*peso;
			x = pow(1.01, y);
			costo = 25*(1 - x);
			//cout << nombre << " " << cedula << " " << peso << " " << precio << " " << costo;	
	}	
	*/
	//cout << nombre << " " << cedula << " " << peso << " " << precio << " " << costo << endl;
